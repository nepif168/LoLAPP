<!DOCTYPE html>
<html>
  <head>
    <title>LoLApp</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <!-- 上部メニューバー -->
    <% if user_signed_in? %>
      <nav class="navbar navbar-default navbar-fixed-top">
        <div class="navbar-header">
          <a class="navbar-brand">LoLApp</a>
        </div>

        <ul class="nav navbar-nav">
          <li><%= link_to "ホーム", users_path %></li>
          <li><%= link_to "データベース", champions_path %></li>
          <li><%= link_to "アイテムセット",  itemsetLists_user_path(current_user) %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right" style="padding-right:10px">
          <li><%= link_to "アカウント設定", edit_user_registration_path %></li>
        </ul>
      </nav>
      <!-- メニューバーここまで -->

      <div class="container">
    <% end %>
      <%= yield %>
    <%if user_signed_in? %>
      </div>
    <% end %>
  </body>
</html>
